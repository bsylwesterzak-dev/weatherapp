import styled from 'styled-components';

const Span = styled.span`
    position: absolute;
    top: 42px;
    left: 40px;
    pointer-events: none;
    font-size: 16px;
    opacity:0;
`;

export default Span;